/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow dev server to be accessed from 127.0.0.1 (e.g. browser tools, previews)
  allowedDevOrigins: ["127.0.0.1"],
};

export default nextConfig;
